/** Phase 4에서 차량 목록/등록으로 채운다. 지금은 보호 라우트 동작 확인용. */
export function VehicleListPage() {
  return (
    <div className="text-muted-foreground text-sm">
      차량 목록은 Phase 4에서 만듭니다. 이 화면이 보인다면 로그인 상태입니다.
    </div>
  )
}
